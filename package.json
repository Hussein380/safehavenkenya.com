{
    "name": "safehaven-monorepo",
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "dev": "npm run dev --workspace=safehaven-frontend",
        "build": "npm run build --workspaces"
    },
    "engines": {
        "node": "20.x"
    }
}